---
date: 2025-10-28
title: Pinia完全入門：Vue.jsの直感的な状態管理ライブラリでアプリケーションを構築する
description: Piniaの基本概念から実装方法まで、初心者向けに詳しく解説。Vuexの後継として設計されたPiniaの特徴、ストアの作成、アクション、ゲッター、プラグインの活用を通じて、Vue.jsアプリケーションでの効率的な状態管理を学ぶ実践的なガイド。
tags:
  - pinia
  - vue
  - state-management
  - store
  - javascript
  - typescript
  - frontend
  - reactive
  - devtools
prev:
  text: '依存性注入（DI）完全入門：疎結合で保守性の高いアプリケーションを構築する'
  link: '/posts/2025/10/27'
next: false
---

# Pinia完全入門：Vue.jsの直感的な状態管理ライブラリでアプリケーションを構築する

[Pinia](https://pinia.vuejs.org/)は、Vue.jsの公式状態管理ライブラリで、Vuexの後継として設計されました。型安全性、直感的なAPI、優れた開発者体験を提供し、Vue.jsアプリケーションでの効率的な状態管理を実現します。この記事では、Piniaの基本概念から実装方法まで、実践的なコード例を通じて詳しく解説します。

## Piniaとは

Piniaは、Vue.jsアプリケーションのための直感的で型安全な状態管理ライブラリです。以下の特徴を持っています：

- **直感的なAPI**: コンポーネントのように親しみやすいストア設計
- **型安全性**: TypeScriptの型推論による自動補完
- **DevTools対応**: Vue DevToolsとの完全統合
- **モジュラー設計**: 複数ストアの自動コード分割
- **軽量**: 約1.5KBの軽量サイズ
- **拡張性**: プラグインによる機能拡張

### Vuexとの比較

| 特徴       | Vuex       | Pinia         |
| ---------- | ---------- | ------------- |
| 型安全性   | 限定的     | 完全対応      |
| API設計    | 複雑       | 直感的        |
| モジュール | 複雑な設定 | 自動分割      |
| DevTools   | 対応       | 完全統合      |
| サイズ     | 重い       | 軽量（1.5KB） |

## プロジェクトのセットアップ

### インストール

```bash
# npm
npm install pinia

# yarn
yarn add pinia

# pnpm
pnpm add pinia
```

### 基本的な設定

```typescript
// main.ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.mount('#app')
```

## 基本的なストアの作成

### カウンターストア

```typescript
// stores/counter.ts
import { defineStore } from 'pinia'

export const useCounterStore = defineStore('counter', {
  // 状態（state）
  state: () => ({
    count: 0,
    name: 'Counter Store',
  }),

  // ゲッター（getters）
  getters: {
    doubleCount: (state) => state.count * 2,
    doubleCountPlusOne: (state) => state.doubleCount + 1,
    // 他のゲッターを参照
    greeting: (state) => `Hello, ${state.name}! Count is ${state.count}`,
  },

  // アクション（actions）
  actions: {
    increment() {
      this.count++
    },
    decrement() {
      this.count--
    },
    incrementBy(amount: number) {
      this.count += amount
    },
    reset() {
      this.count = 0
    },
    updateName(newName: string) {
      this.name = newName
    },
  },
})
```

### コンポーネントでの使用

```vue
<!-- Counter.vue -->
<template>
  <div class="counter">
    <h2>{{ counterStore.name }}</h2>
    <p>Count: {{ counterStore.count }}</p>
    <p>Double Count: {{ counterStore.doubleCount }}</p>
    <p>Greeting: {{ counterStore.greeting }}</p>

    <div class="buttons">
      <button @click="counterStore.increment">+</button>
      <button @click="counterStore.decrement">-</button>
      <button @click="counterStore.incrementBy(5)">+5</button>
      <button @click="counterStore.reset">Reset</button>
    </div>

    <input v-model="newName" placeholder="Enter new name" @keyup.enter="updateName" />
    <button @click="updateName">Update Name</button>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useCounterStore } from '@/stores/counter'

const counterStore = useCounterStore()
const newName = ref('')

const updateName = () => {
  if (newName.value.trim()) {
    counterStore.updateName(newName.value.trim())
    newName.value = ''
  }
}
</script>

<style scoped>
.counter {
  text-align: center;
  padding: 20px;
}

.buttons {
  margin: 20px 0;
}

.buttons button {
  margin: 0 5px;
  padding: 10px 15px;
  font-size: 16px;
}

input {
  margin: 10px;
  padding: 8px;
  font-size: 14px;
}
</style>
```

## 複雑なストアの実装

### ユーザー管理ストア

```typescript
// stores/user.ts
import { defineStore } from 'pinia'

export interface User {
  id: string
  name: string
  email: string
  avatar?: string
  createdAt: Date
}

export interface UserState {
  users: User[]
  currentUser: User | null
  loading: boolean
  error: string | null
}

export const useUserStore = defineStore('user', {
  state: (): UserState => ({
    users: [],
    currentUser: null,
    loading: false,
    error: null,
  }),

  getters: {
    // 全ユーザー数
    userCount: (state) => state.users.length,

    // アクティブユーザー（現在のユーザー）
    isLoggedIn: (state) => state.currentUser !== null,

    // ユーザー検索
    getUserById: (state) => (id: string) => state.users.find((user) => user.id === id),

    // ユーザー名で検索
    getUsersByName: (state) => (name: string) =>
      state.users.filter((user) => user.name.toLowerCase().includes(name.toLowerCase())),

    // 最近作成されたユーザー
    recentUsers: (state) =>
      [...state.users].sort((a, b) => b.createdAt.getTime() - a.createdAt.getTime()).slice(0, 5),
  },

  actions: {
    // ユーザー一覧を取得
    async fetchUsers() {
      this.loading = true
      this.error = null

      try {
        // 実際のAPI呼び出し
        const response = await fetch('/api/users')
        if (!response.ok) {
          throw new Error('Failed to fetch users')
        }

        const users = await response.json()
        this.users = users.map((user: any) => ({
          ...user,
          createdAt: new Date(user.createdAt),
        }))
      } catch (error) {
        this.error = error instanceof Error ? error.message : 'Unknown error'
      } finally {
        this.loading = false
      }
    },

    // ユーザーを作成
    async createUser(userData: Omit<User, 'id' | 'createdAt'>) {
      this.loading = true
      this.error = null

      try {
        const response = await fetch('/api/users', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(userData),
        })

        if (!response.ok) {
          throw new Error('Failed to create user')
        }

        const newUser = await response.json()
        this.users.push({
          ...newUser,
          createdAt: new Date(newUser.createdAt),
        })

        return newUser
      } catch (error) {
        this.error = error instanceof Error ? error.message : 'Unknown error'
        throw error
      } finally {
        this.loading = false
      }
    },

    // ユーザーを更新
    async updateUser(id: string, userData: Partial<User>) {
      this.loading = true
      this.error = null

      try {
        const response = await fetch(`/api/users/${id}`, {
          method: 'PUT',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(userData),
        })

        if (!response.ok) {
          throw new Error('Failed to update user')
        }

        const updatedUser = await response.json()
        const index = this.users.findIndex((user) => user.id === id)
        if (index !== -1) {
          this.users[index] = {
            ...updatedUser,
            createdAt: new Date(updatedUser.createdAt),
          }
        }

        return updatedUser
      } catch (error) {
        this.error = error instanceof Error ? error.message : 'Unknown error'
        throw error
      } finally {
        this.loading = false
      }
    },

    // ユーザーを削除
    async deleteUser(id: string) {
      this.loading = true
      this.error = null

      try {
        const response = await fetch(`/api/users/${id}`, {
          method: 'DELETE',
        })

        if (!response.ok) {
          throw new Error('Failed to delete user')
        }

        this.users = this.users.filter((user) => user.id !== id)

        // 現在のユーザーが削除された場合
        if (this.currentUser?.id === id) {
          this.currentUser = null
        }
      } catch (error) {
        this.error = error instanceof Error ? error.message : 'Unknown error'
        throw error
      } finally {
        this.loading = false
      }
    },

    // ログイン
    login(user: User) {
      this.currentUser = user
    },

    // ログアウト
    logout() {
      this.currentUser = null
    },

    // エラーをクリア
    clearError() {
      this.error = null
    },
  },
})
```

### ユーザー管理コンポーネント

```vue
<!-- UserManagement.vue -->
<template>
  <div class="user-management">
    <h2>ユーザー管理</h2>

    <!-- エラー表示 -->
    <div v-if="userStore.error" class="error">
      {{ userStore.error }}
      <button @click="userStore.clearError">×</button>
    </div>

    <!-- ローディング表示 -->
    <div v-if="userStore.loading" class="loading">読み込み中...</div>

    <!-- ユーザー一覧 -->
    <div class="user-list">
      <div class="user-item" v-for="user in userStore.users" :key="user.id">
        <img :src="user.avatar || '/default-avatar.png'" :alt="user.name" />
        <div class="user-info">
          <h3>{{ user.name }}</h3>
          <p>{{ user.email }}</p>
          <small>{{ formatDate(user.createdAt) }}</small>
        </div>
        <div class="user-actions">
          <button @click="editUser(user)">編集</button>
          <button @click="deleteUser(user.id)">削除</button>
        </div>
      </div>
    </div>

    <!-- ユーザー作成フォーム -->
    <form @submit.prevent="createUser" class="user-form">
      <h3>新しいユーザーを作成</h3>
      <input v-model="newUser.name" placeholder="名前" required />
      <input v-model="newUser.email" type="email" placeholder="メールアドレス" required />
      <input v-model="newUser.avatar" placeholder="アバターURL" />
      <button type="submit" :disabled="userStore.loading">作成</button>
    </form>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { useUserStore, type User } from '@/stores/user'

const userStore = useUserStore()

const newUser = ref({
  name: '',
  email: '',
  avatar: '',
})

const formatDate = (date: Date) => {
  return date.toLocaleDateString('ja-JP')
}

const createUser = async () => {
  try {
    await userStore.createUser(newUser.value)
    newUser.value = { name: '', email: '', avatar: '' }
  } catch (error) {
    console.error('Failed to create user:', error)
  }
}

const editUser = (user: User) => {
  // 編集モーダルを開くなどの処理
  console.log('Edit user:', user)
}

const deleteUser = async (id: string) => {
  if (confirm('このユーザーを削除しますか？')) {
    try {
      await userStore.deleteUser(id)
    } catch (error) {
      console.error('Failed to delete user:', error)
    }
  }
}

onMounted(() => {
  userStore.fetchUsers()
})
</script>

<style scoped>
.user-management {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
}

.error {
  background: #fee;
  color: #c33;
  padding: 10px;
  border-radius: 4px;
  margin-bottom: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.loading {
  text-align: center;
  padding: 20px;
  color: #666;
}

.user-list {
  margin-bottom: 30px;
}

.user-item {
  display: flex;
  align-items: center;
  padding: 15px;
  border: 1px solid #ddd;
  border-radius: 8px;
  margin-bottom: 10px;
}

.user-item img {
  width: 50px;
  height: 50px;
  border-radius: 50%;
  margin-right: 15px;
}

.user-info {
  flex: 1;
}

.user-info h3 {
  margin: 0 0 5px 0;
}

.user-info p {
  margin: 0 0 5px 0;
  color: #666;
}

.user-info small {
  color: #999;
}

.user-actions {
  display: flex;
  gap: 10px;
}

.user-actions button {
  padding: 5px 10px;
  border: 1px solid #ddd;
  background: white;
  border-radius: 4px;
  cursor: pointer;
}

.user-actions button:hover {
  background: #f5f5f5;
}

.user-form {
  background: #f9f9f9;
  padding: 20px;
  border-radius: 8px;
}

.user-form input {
  width: 100%;
  padding: 10px;
  margin-bottom: 10px;
  border: 1px solid #ddd;
  border-radius: 4px;
}

.user-form button {
  padding: 10px 20px;
  background: #007bff;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
}

.user-form button:disabled {
  background: #ccc;
  cursor: not-allowed;
}
</style>
```

## ストア間の連携

### ショッピングカートストア

```typescript
// stores/cart.ts
import { defineStore } from 'pinia'
import { useUserStore } from './user'

export interface CartItem {
  id: string
  name: string
  price: number
  quantity: number
  image?: string
}

export const useCartStore = defineStore('cart', {
  state: () => ({
    items: [] as CartItem[],
    isOpen: false,
  }),

  getters: {
    // カート内のアイテム数
    itemCount: (state) => state.items.reduce((total, item) => total + item.quantity, 0),

    // 合計金額
    totalPrice: (state) =>
      state.items.reduce((total, item) => total + item.price * item.quantity, 0),

    // カートが空かどうか
    isEmpty: (state) => state.items.length === 0,

    // 特定のアイテムがカートにあるかどうか
    hasItem: (state) => (itemId: string) => state.items.some((item) => item.id === itemId),
  },

  actions: {
    // アイテムを追加
    addItem(item: Omit<CartItem, 'quantity'>) {
      const existingItem = this.items.find((i) => i.id === item.id)

      if (existingItem) {
        existingItem.quantity++
      } else {
        this.items.push({ ...item, quantity: 1 })
      }
    },

    // アイテムを削除
    removeItem(itemId: string) {
      this.items = this.items.filter((item) => item.id !== itemId)
    },

    // 数量を更新
    updateQuantity(itemId: string, quantity: number) {
      const item = this.items.find((i) => i.id === itemId)
      if (item) {
        if (quantity <= 0) {
          this.removeItem(itemId)
        } else {
          item.quantity = quantity
        }
      }
    },

    // カートをクリア
    clearCart() {
      this.items = []
    },

    // カートを開く/閉じる
    toggleCart() {
      this.isOpen = !this.isOpen
    },

    // チェックアウト
    async checkout() {
      const userStore = useUserStore()

      if (!userStore.isLoggedIn) {
        throw new Error('ログインが必要です')
      }

      try {
        const response = await fetch('/api/checkout', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            Authorization: `Bearer ${userStore.currentUser?.id}`,
          },
          body: JSON.stringify({
            items: this.items,
            userId: userStore.currentUser?.id,
          }),
        })

        if (!response.ok) {
          throw new Error('チェックアウトに失敗しました')
        }

        // チェックアウト成功後、カートをクリア
        this.clearCart()
        this.isOpen = false

        return await response.json()
      } catch (error) {
        console.error('Checkout error:', error)
        throw error
      }
    },
  },
})
```

### 商品ストア

```typescript
// stores/product.ts
import { defineStore } from 'pinia'

export interface Product {
  id: string
  name: string
  price: number
  description: string
  image: string
  category: string
  inStock: boolean
  rating: number
}

export const useProductStore = defineStore('product', {
  state: () => ({
    products: [] as Product[],
    loading: false,
    error: null as string | null,
    searchQuery: '',
    selectedCategory: 'all',
    sortBy: 'name' as 'name' | 'price' | 'rating',
  }),

  getters: {
    // フィルタリングされた商品
    filteredProducts: (state) => {
      let filtered = state.products

      // カテゴリでフィルタリング
      if (state.selectedCategory !== 'all') {
        filtered = filtered.filter((product) => product.category === state.selectedCategory)
      }

      // 検索クエリでフィルタリング
      if (state.searchQuery) {
        const query = state.searchQuery.toLowerCase()
        filtered = filtered.filter(
          (product) =>
            product.name.toLowerCase().includes(query) ||
            product.description.toLowerCase().includes(query),
        )
      }

      // ソート
      return filtered.sort((a, b) => {
        switch (state.sortBy) {
          case 'price':
            return a.price - b.price
          case 'rating':
            return b.rating - a.rating
          default:
            return a.name.localeCompare(b.name)
        }
      })
    },

    // カテゴリ一覧
    categories: (state) => {
      const categories = new Set(state.products.map((product) => product.category))
      return Array.from(categories)
    },

    // 在庫切れ商品
    outOfStockProducts: (state) => state.products.filter((product) => !product.inStock),
  },

  actions: {
    // 商品一覧を取得
    async fetchProducts() {
      this.loading = true
      this.error = null

      try {
        const response = await fetch('/api/products')
        if (!response.ok) {
          throw new Error('商品の取得に失敗しました')
        }

        this.products = await response.json()
      } catch (error) {
        this.error = error instanceof Error ? error.message : 'Unknown error'
      } finally {
        this.loading = false
      }
    },

    // 検索クエリを設定
    setSearchQuery(query: string) {
      this.searchQuery = query
    },

    // カテゴリを設定
    setCategory(category: string) {
      this.selectedCategory = category
    },

    // ソート方法を設定
    setSortBy(sortBy: 'name' | 'price' | 'rating') {
      this.sortBy = sortBy
    },
  },
})
```

## プラグインの活用

### 永続化プラグイン

```typescript
// plugins/persistence.ts
import { PiniaPluginContext } from 'pinia'

export function persistencePlugin({ store }: PiniaPluginContext) {
  // ストアの状態をローカルストレージに保存
  const storageKey = `pinia-${store.$id}`

  // 初期化時にローカルストレージから復元
  const stored = localStorage.getItem(storageKey)
  if (stored) {
    store.$patch(JSON.parse(stored))
  }

  // 状態変更時にローカルストレージに保存
  store.$subscribe((mutation, state) => {
    localStorage.setItem(storageKey, JSON.stringify(state))
  })
}

// main.ts
import { createPinia } from 'pinia'
import { persistencePlugin } from './plugins/persistence'

const pinia = createPinia()
pinia.use(persistencePlugin)
```

### ログプラグイン

```typescript
// plugins/logger.ts
import { PiniaPluginContext } from 'pinia'

export function loggerPlugin({ store }: PiniaPluginContext) {
  // アクション実行前のログ
  store.$onAction(({ name, args, after, onError }) => {
    console.log(`Action ${name} started with args:`, args)

    after((result) => {
      console.log(`Action ${name} completed with result:`, result)
    })

    onError((error) => {
      console.error(`Action ${name} failed with error:`, error)
    })
  })

  // 状態変更のログ
  store.$subscribe((mutation, state) => {
    console.log(`State changed in ${store.$id}:`, mutation, state)
  })
}
```

## TypeScriptでの型安全性

### 型安全なストア

```typescript
// stores/typed-store.ts
import { defineStore } from 'pinia'

interface User {
  id: string
  name: string
  email: string
}

interface UserState {
  users: User[]
  currentUser: User | null
  loading: boolean
}

export const useTypedStore = defineStore('typed', {
  state: (): UserState => ({
    users: [],
    currentUser: null,
    loading: false,
  }),

  getters: {
    // 型安全なゲッター
    userCount: (state): number => state.users.length,

    // パラメータ付きゲッター
    getUserById:
      (state) =>
      (id: string): User | undefined =>
        state.users.find((user) => user.id === id),

    // 他のゲッターを参照
    currentUserInfo: (state): string =>
      state.currentUser
        ? `${state.currentUser.name} (${state.currentUser.email})`
        : 'Not logged in',
  },

  actions: {
    // 型安全なアクション
    async fetchUsers(): Promise<void> {
      this.loading = true
      try {
        const response = await fetch('/api/users')
        this.users = await response.json()
      } finally {
        this.loading = false
      }
    },

    // パラメータ付きアクション
    async createUser(userData: Omit<User, 'id'>): Promise<User> {
      const response = await fetch('/api/users', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(userData),
      })

      const newUser = await response.json()
      this.users.push(newUser)
      return newUser
    },

    // 戻り値のあるアクション
    login(email: string, password: string): boolean {
      const user = this.users.find((u) => u.email === email)
      if (user) {
        this.currentUser = user
        return true
      }
      return false
    },
  },
})
```

## まとめ

Piniaは、Vue.jsアプリケーションの状態管理を効率化する強力なライブラリです。主なポイントは以下の通りです：

### 主要な特徴

- **直感的なAPI**: コンポーネントのように親しみやすい設計
- **型安全性**: TypeScriptの完全サポート
- **DevTools対応**: Vue DevToolsとの完全統合
- **軽量**: 約1.5KBの軽量サイズ
- **拡張性**: プラグインによる機能拡張

### 実装のポイント

- **適切なストア設計**: 単一責任の原則に従ったストア設計
- **型安全性**: TypeScriptの型システムを活用
- **ストア間の連携**: 適切な依存関係の管理
- **プラグインの活用**: 永続化やログなどの機能拡張

### 実践的な用途

- **大規模アプリケーション**: 複雑な状態の管理
- **リアルタイムアプリケーション**: リアクティブな状態更新
- **SPA**: シングルページアプリケーションの状態管理
- **モバイルアプリ**: Vue Nativeでの状態管理

[Pinia公式サイト](https://pinia.vuejs.org/)を参考に、Vue.jsアプリケーションでの効率的な状態管理を学びましょう。直感的なAPIと優れた型安全性により、保守性の高いアプリケーションを構築できます。
